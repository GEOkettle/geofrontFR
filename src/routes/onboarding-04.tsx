import { createFileRoute } from '@tanstack/react-router'

import Onboarding04 from '#/features/seed/pages/Onboarding04'

export const Route = createFileRoute('/onboarding-04')({
  component: Onboarding04,
})
