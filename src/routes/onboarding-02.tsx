import { createFileRoute } from '@tanstack/react-router'

import Onboarding02 from '#/features/seed/pages/Onboarding02'

export const Route = createFileRoute('/onboarding-02')({
  component: Onboarding02,
})
