import { createFileRoute } from '@tanstack/react-router'

import Onboarding03 from '#/features/seed/pages/Onboarding03'

export const Route = createFileRoute('/onboarding-03')({
  component: Onboarding03,
})
